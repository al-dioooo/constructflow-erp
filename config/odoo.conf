[options]
addons_path = /usr/lib/python3/dist-packages/odoo/addons,/mnt/extra-addons
data_dir = /var/lib/odoo
proxy_mode = True
; Set to False after the first database (constructflow_erp) is created.
list_db = True
; Single-process mode — tune to multi-worker once load is measured.
workers = 0
max_cron_threads = 1
db_name = constructflow_erp
